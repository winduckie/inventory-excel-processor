# Exclude from automatic indexing - only read when explicitly needed
# Core code files (run_processor_split.py, combined_processor*.py, config/) are NOT excluded
# so they can be edited normally and are available for codebase understanding

# Data directories - only read when referenced
input/
processed_data/
archive/

# Logs and cache - never needed for context
logs/
__pycache__/

# Extra code files - only read when specifically asked for
extra_code/

# Documentation - exclude to reduce tokens (can be read if needed)
README*.md

