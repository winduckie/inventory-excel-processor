# Exclude everything from automatic indexing
# Agents should only read files when explicitly needed based on the question
# This maximizes token efficiency while still allowing explicit file reads

# All data directories
input/
processed_data/
archive/

# Logs and cache
logs/
__pycache__/

# Extra code files
extra_code/

# Documentation
README*.md

# Core code files - exclude from auto-indexing but can be read when needed
# (Agents will read these files explicitly when working on tasks)
run_processor_split.py
combined_processor_split.py
combined_processor.py
config/

